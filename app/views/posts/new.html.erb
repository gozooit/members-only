<% if @post.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@post.errors.count, "error") %> prohibited this article from being saved:</h2>

    <ul>
      <% @post.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<%= form_with(model: @post, data: { turbo: "false" }) do |form| %>
  <div class="title">
    Title
    <%= form.text_field :title, value: @post.title %>
  </div>
  <div class="Body">
    Body
    <%= form.text_field :body, value: @post.body %>
  </div>
  <%= form.submit "Create" %>
<% end %>
