<h1>Posts</h1>

<ul>
  <% @posts.each do |post| %>
    <h1><%= post.title %></h1>

    <p><%= post.body %></p>
    <% if user_signed_in? %>
      <p><%= User.find(post.user_id).email %></p>
    <% end %>
  <% end %>
</ul>

<%= link_to "New Post", new_post_path %>
